'use strict';

// TODO implement add function

const add = (a, b) => a + b;
// TODO implement sub function
const sub = (a, b) => a - b;
// TODO implement mul function
const mul = (a, b) => a * b;
// TODO implement div function
const div = (a, b) => a / b;
// TODO implement per function
const per = a => a / 100;
// TODO export each of the functions above
module.exports = {
    add: add,
    sub: sub,
    mul: mul,
    div: div,
    per: per
};